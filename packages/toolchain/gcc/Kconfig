config PACKAGE_TOOLCHAIN_GCC
	bool "GCC"
	depends on PACKAGE_TOOLCHAIN
	select PACKAGE_TOOLCHAIN_GLIBC if GLIBC
	select PACKAGE_TOOLCHAIN_KLIBC if KLIBC
	select PACKAGE_TOOLCHAIN_UCLIBC if UCLIBC
	default n

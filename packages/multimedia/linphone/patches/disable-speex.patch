diff --git a/configure b/configure
index 2856d12..be1b04f 100755
--- a/configure
+++ b/configure
@@ -779,8 +779,6 @@ ARMBUILD_TRUE
 BUILD_CONSOLE_FALSE
 BUILD_CONSOLE_TRUE
 X11_LIBS
-SPEEX_LIBS
-SPEEX_CFLAGS
 READLINE_LIBS
 READLINE_CFLAGS
 EXOSIP_LIBS
@@ -1018,8 +1016,6 @@ OSIP_CFLAGS
 OSIP_LIBS
 OPENSSL_CFLAGS
 OPENSSL_LIBS
-SPEEX_CFLAGS
-SPEEX_LIBS
 MEDIASTREAMER_CFLAGS
 MEDIASTREAMER_LIBS
 ORTP_CFLAGS
@@ -1721,9 +1717,6 @@ Some influential environment variables:
               C compiler flags for OPENSSL, overriding pkg-config
   OPENSSL_LIBS
               linker flags for OPENSSL, overriding pkg-config
-  SPEEX_CFLAGS
-              C compiler flags for SPEEX, overriding pkg-config
-  SPEEX_LIBS  linker flags for SPEEX, overriding pkg-config
   MEDIASTREAMER_CFLAGS
               C compiler flags for MEDIASTREAMER, overriding pkg-config
   MEDIASTREAMER_LIBS
@@ -2425,7 +2418,7 @@ $as_echo "$as_me: loading site script $ac_site_file" >&6;}
       || { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
 as_fn_error $? "failed to load site script $ac_site_file
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
   fi
 done
 
@@ -2566,7 +2559,7 @@ fi
 $as_echo "$ac_cv_build" >&6; }
 case $ac_cv_build in
 *-*-*) ;;
-*) as_fn_error $? "invalid value of canonical build" "$LINENO" 5 ;;
+*) as_fn_error $? "invalid value of canonical build" "$LINENO" 5;;
 esac
 build=$ac_cv_build
 ac_save_IFS=$IFS; IFS='-'
@@ -2599,7 +2592,7 @@ fi
 $as_echo "$ac_cv_host" >&6; }
 case $ac_cv_host in
 *-*-*) ;;
-*) as_fn_error $? "invalid value of canonical host" "$LINENO" 5 ;;
+*) as_fn_error $? "invalid value of canonical host" "$LINENO" 5;;
 esac
 host=$ac_cv_host
 ac_save_IFS=$IFS; IFS='-'
@@ -2632,7 +2625,7 @@ fi
 $as_echo "$ac_cv_target" >&6; }
 case $ac_cv_target in
 *-*-*) ;;
-*) as_fn_error $? "invalid value of canonical target" "$LINENO" 5 ;;
+*) as_fn_error $? "invalid value of canonical target" "$LINENO" 5;;
 esac
 target=$ac_cv_target
 ac_save_IFS=$IFS; IFS='-'
@@ -2786,11 +2779,11 @@ am_lf='
 '
 case `pwd` in
   *[\\\"\#\$\&\'\`$am_lf]*)
-    as_fn_error $? "unsafe absolute working directory name" "$LINENO" 5 ;;
+    as_fn_error $? "unsafe absolute working directory name" "$LINENO" 5;;
 esac
 case $srcdir in
   *[\\\"\#\$\&\'\`$am_lf\ \	]*)
-    as_fn_error $? "unsafe srcdir value: \`$srcdir'" "$LINENO" 5 ;;
+    as_fn_error $? "unsafe srcdir value: \`$srcdir'" "$LINENO" 5;;
 esac
 
 # Do `set' in a subshell so we don't clobber the current shell's
@@ -3538,7 +3531,7 @@ fi
 test -z "$CC" && { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
 as_fn_error $? "no acceptable C compiler found in \$PATH
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 
 # Provide some information about the compiler.
 $as_echo "$as_me:${as_lineno-$LINENO}: checking for C compiler version" >&5
@@ -3653,7 +3646,7 @@ sed 's/^/| /' conftest.$ac_ext >&5
 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
 as_fn_error 77 "C compiler cannot create executables
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 else
   { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 $as_echo "yes" >&6; }
@@ -3696,7 +3689,7 @@ else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
 as_fn_error $? "cannot compute suffix of executables: cannot compile and link
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest conftest$ac_cv_exeext
 { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_exeext" >&5
@@ -3755,7 +3748,7 @@ $as_echo "$ac_try_echo"; } >&5
 $as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
 as_fn_error $? "cannot run C compiled programs.
 If you meant to cross compile, use \`--host'.
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
     fi
   fi
 fi
@@ -3807,7 +3800,7 @@ sed 's/^/| /' conftest.$ac_ext >&5
 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
 as_fn_error $? "cannot compute suffix of object files: cannot compile
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest.$ac_cv_objext conftest.$ac_ext
 fi
@@ -4499,7 +4492,7 @@ fi
 test -z "$CC" && { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
 as_fn_error $? "no acceptable C compiler found in \$PATH
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 
 # Provide some information about the compiler.
 $as_echo "$as_me:${as_lineno-$LINENO}: checking for C compiler version" >&5
@@ -5208,7 +5201,7 @@ fi
 test -z "$CC" && { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
 as_fn_error $? "no acceptable C compiler found in \$PATH
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 
 # Provide some information about the compiler.
 $as_echo "$as_me:${as_lineno-$LINENO}: checking for C compiler version" >&5
@@ -5705,7 +5698,7 @@ else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
 as_fn_error $? "C preprocessor \"$CPP\" fails sanity check
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 fi
 
 ac_ext=c
@@ -7451,13 +7444,13 @@ if test "${lt_cv_nm_interface+set}" = set; then :
 else
   lt_cv_nm_interface="BSD nm"
   echo "int some_variable = 0;" > conftest.$ac_ext
-  (eval echo "\"\$as_me:7454: $ac_compile\"" >&5)
+  (eval echo "\"\$as_me:7447: $ac_compile\"" >&5)
   (eval "$ac_compile" 2>conftest.err)
   cat conftest.err >&5
-  (eval echo "\"\$as_me:7457: $NM \\\"conftest.$ac_objext\\\"\"" >&5)
+  (eval echo "\"\$as_me:7450: $NM \\\"conftest.$ac_objext\\\"\"" >&5)
   (eval "$NM \"conftest.$ac_objext\"" 2>conftest.err > conftest.out)
   cat conftest.err >&5
-  (eval echo "\"\$as_me:7460: output\"" >&5)
+  (eval echo "\"\$as_me:7453: output\"" >&5)
   cat conftest.out >&5
   if $GREP 'External.*some_variable' conftest.out > /dev/null; then
     lt_cv_nm_interface="MS dumpbin"
@@ -8659,7 +8652,7 @@ ia64-*-hpux*)
   ;;
 *-*-irix6*)
   # Find out which ABI we are using.
-  echo '#line 8662 "configure"' > conftest.$ac_ext
+  echo '#line 8655 "configure"' > conftest.$ac_ext
   if { { eval echo "\"\$as_me\":${as_lineno-$LINENO}: \"$ac_compile\""; } >&5
   (eval $ac_compile) 2>&5
   ac_status=$?
@@ -9936,11 +9929,11 @@ else
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:9939: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:9932: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>conftest.err)
    ac_status=$?
    cat conftest.err >&5
-   echo "$as_me:9943: \$? = $ac_status" >&5
+   echo "$as_me:9936: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s "$ac_outfile"; then
      # The compiler can only warn and ignore the option if not recognized
      # So say no if there are warnings other than the usual output.
@@ -10275,11 +10268,11 @@ else
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:10278: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:10271: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>conftest.err)
    ac_status=$?
    cat conftest.err >&5
-   echo "$as_me:10282: \$? = $ac_status" >&5
+   echo "$as_me:10275: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s "$ac_outfile"; then
      # The compiler can only warn and ignore the option if not recognized
      # So say no if there are warnings other than the usual output.
@@ -10380,11 +10373,11 @@ else
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:10383: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:10376: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>out/conftest.err)
    ac_status=$?
    cat out/conftest.err >&5
-   echo "$as_me:10387: \$? = $ac_status" >&5
+   echo "$as_me:10380: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s out/conftest2.$ac_objext
    then
      # The compiler can only warn and ignore the option if not recognized
@@ -10435,11 +10428,11 @@ else
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:10438: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:10431: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>out/conftest.err)
    ac_status=$?
    cat out/conftest.err >&5
-   echo "$as_me:10442: \$? = $ac_status" >&5
+   echo "$as_me:10435: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s out/conftest2.$ac_objext
    then
      # The compiler can only warn and ignore the option if not recognized
@@ -12819,7 +12812,7 @@ else
   lt_dlunknown=0; lt_dlno_uscore=1; lt_dlneed_uscore=2
   lt_status=$lt_dlunknown
   cat > conftest.$ac_ext <<_LT_EOF
-#line 12822 "configure"
+#line 12815 "configure"
 #include "confdefs.h"
 
 #if HAVE_DLFCN_H
@@ -12915,7 +12908,7 @@ else
   lt_dlunknown=0; lt_dlno_uscore=1; lt_dlneed_uscore=2
   lt_status=$lt_dlunknown
   cat > conftest.$ac_ext <<_LT_EOF
-#line 12918 "configure"
+#line 12911 "configure"
 #include "confdefs.h"
 
 #if HAVE_DLFCN_H
@@ -15068,7 +15061,7 @@ if test "${enable_console_ui+set}" = set; then :
   enableval=$enable_console_ui; case "${enableval}" in
         yes) console_ui=true ;;
         no)  console_ui=false ;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-console_ui" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-console_ui" "$LINENO" 5 ;;
       esac
 else
   console_ui=true
@@ -15080,7 +15073,7 @@ if test "${enable_gtk_ui+set}" = set; then :
   enableval=$enable_gtk_ui; case "${enableval}" in
         yes) gtk_ui=true ;;
         no)  gtk_ui=false ;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-gtk_ui" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-gtk_ui" "$LINENO" 5 ;;
       esac
 else
   gtk_ui=true
@@ -15170,7 +15163,7 @@ and LIBGTK_LIBS to avoid the need to call pkg-config.
 See the pkg-config man page for more details.
 
 To get pkg-config, see <http://pkg-config.freedesktop.org/>.
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 else
 	LIBGTK_CFLAGS=$pkg_cv_LIBGTK_CFLAGS
 	LIBGTK_LIBS=$pkg_cv_LIBGTK_LIBS
@@ -15260,7 +15253,7 @@ and LIBGTKMAC_LIBS to avoid the need to call pkg-config.
 See the pkg-config man page for more details.
 
 To get pkg-config, see <http://pkg-config.freedesktop.org/>.
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 else
 	LIBGTKMAC_CFLAGS=$pkg_cv_LIBGTKMAC_CFLAGS
 	LIBGTKMAC_LIBS=$pkg_cv_LIBGTKMAC_LIBS
@@ -15309,7 +15302,7 @@ if test "${enable_relativeprefix+set}" = set; then :
   enableval=$enable_relativeprefix; case "${enableval}" in
         yes) relativeprefix=yes ;;
         no)  relativeprefix=no ;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-relativeprefix" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-relativeprefix" "$LINENO" 5 ;;
       esac
 else
   relativeprefix=auto
@@ -15321,7 +15314,7 @@ if test "${enable_date+set}" = set; then :
   enableval=$enable_date; case "${enableval}" in
         yes) use_date=yes ;;
         no)  use_date=no ;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-date" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-date" "$LINENO" 5 ;;
       esac
 else
   use_date=no
@@ -15340,7 +15333,7 @@ if test "${enable_ipv6+set}" = set; then :
   enableval=$enable_ipv6; case "${enableval}" in
         yes)  ipv6=true;;
         no)   ipv6=false;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-ipv6" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-ipv6" "$LINENO" 5 ;;
       esac
 else
   ipv6=true
@@ -15357,7 +15350,7 @@ if test "${enable_truespeech+set}" = set; then :
   enableval=$enable_truespeech; case "${enableval}" in
         yes)  truespeech=true;;
         no)   truespeech=false;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-truespeech" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-truespeech" "$LINENO" 5 ;;
       esac
 else
   truespeech=false
@@ -15386,7 +15379,7 @@ $as_echo "#define ENABLE_NONSTANDARD_GSM 1" >>confdefs.h
 
 	 ;;
         no)  exotic_gsm=no ;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-nonstandard-gsm" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-nonstandard-gsm" "$LINENO" 5 ;;
       esac
 else
   exotic_gsm=no
@@ -15698,7 +15691,7 @@ if test "${enable_ssl+set}" = set; then :
   enableval=$enable_ssl; case "${enableval}" in
 		yes) build_ssl=true ;;
 		no)  build_ssl=false ;;
-		*) as_fn_error $? "bad value ${enableval} for --enable-ssl" "$LINENO" 5  ;;
+		*) as_fn_error $? "bad value ${enableval} for --enable-ssl" "$LINENO" 5 ;;
 		esac
 else
   build_ssl=false
@@ -15787,7 +15780,7 @@ and OPENSSL_LIBS to avoid the need to call pkg-config.
 See the pkg-config man page for more details.
 
 To get pkg-config, see <http://pkg-config.freedesktop.org/>.
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 else
 	OPENSSL_CFLAGS=$pkg_cv_OPENSSL_CFLAGS
 	OPENSSL_LIBS=$pkg_cv_OPENSSL_LIBS
@@ -16304,107 +16297,16 @@ $as_echo "#define AC_APPLE_UNIVERSAL_BUILD 1" >>confdefs.h
      ;; #(
    *)
      as_fn_error $? "unknown endianness
- presetting ac_cv_c_bigendian=no (or yes) will help" "$LINENO" 5  ;;
+ presetting ac_cv_c_bigendian=no (or yes) will help" "$LINENO" 5 ;;
  esac
 
 
-
-
-pkg_failed=no
-{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for SPEEX" >&5
-$as_echo_n "checking for SPEEX... " >&6; }
-
-if test -n "$SPEEX_CFLAGS"; then
-    pkg_cv_SPEEX_CFLAGS="$SPEEX_CFLAGS"
- elif test -n "$PKG_CONFIG"; then
-    if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"speex >= 1.1.6\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "speex >= 1.1.6") 2>&5
-  ac_status=$?
-  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
-  test $ac_status = 0; }; then
-  pkg_cv_SPEEX_CFLAGS=`$PKG_CONFIG --cflags "speex >= 1.1.6" 2>/dev/null`
-else
-  pkg_failed=yes
-fi
- else
-    pkg_failed=untried
-fi
-if test -n "$SPEEX_LIBS"; then
-    pkg_cv_SPEEX_LIBS="$SPEEX_LIBS"
- elif test -n "$PKG_CONFIG"; then
-    if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"speex >= 1.1.6\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "speex >= 1.1.6") 2>&5
-  ac_status=$?
-  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
-  test $ac_status = 0; }; then
-  pkg_cv_SPEEX_LIBS=`$PKG_CONFIG --libs "speex >= 1.1.6" 2>/dev/null`
-else
-  pkg_failed=yes
-fi
- else
-    pkg_failed=untried
-fi
-
-
-
-if test $pkg_failed = yes; then
-   	{ $as_echo "$as_me:${as_lineno-$LINENO}: result: no" >&5
-$as_echo "no" >&6; }
-
-if $PKG_CONFIG --atleast-pkgconfig-version 0.20; then
-        _pkg_short_errors_supported=yes
-else
-        _pkg_short_errors_supported=no
-fi
-        if test $_pkg_short_errors_supported = yes; then
-	        SPEEX_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors "speex >= 1.1.6" 2>&1`
-        else
-	        SPEEX_PKG_ERRORS=`$PKG_CONFIG --print-errors "speex >= 1.1.6" 2>&1`
-        fi
-	# Put the nasty error message in config.log where it belongs
-	echo "$SPEEX_PKG_ERRORS" >&5
-
-	as_fn_error $? "Package requirements (speex >= 1.1.6) were not met:
-
-$SPEEX_PKG_ERRORS
-
-Consider adjusting the PKG_CONFIG_PATH environment variable if you
-installed software in a non-standard prefix.
-
-Alternatively, you may set the environment variables SPEEX_CFLAGS
-and SPEEX_LIBS to avoid the need to call pkg-config.
-See the pkg-config man page for more details." "$LINENO" 5
-elif test $pkg_failed = untried; then
-     	{ $as_echo "$as_me:${as_lineno-$LINENO}: result: no" >&5
-$as_echo "no" >&6; }
-	{ { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
-$as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
-as_fn_error $? "The pkg-config script could not be found or is too old.  Make sure it
-is in your PATH or set the PKG_CONFIG environment variable to the full
-path to pkg-config.
-
-Alternatively, you may set the environment variables SPEEX_CFLAGS
-and SPEEX_LIBS to avoid the need to call pkg-config.
-See the pkg-config man page for more details.
-
-To get pkg-config, see <http://pkg-config.freedesktop.org/>.
-See \`config.log' for more details" "$LINENO" 5 ; }
-else
-	SPEEX_CFLAGS=$pkg_cv_SPEEX_CFLAGS
-	SPEEX_LIBS=$pkg_cv_SPEEX_LIBS
-        { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
-$as_echo "yes" >&6; }
-	build_speex=yes
-fi
-
 # Check whether --enable-video was given.
 if test "${enable_video+set}" = set; then :
   enableval=$enable_video; case "${enableval}" in
 		yes) video=true ;;
 		no)  video=false ;;
-		*) as_fn_error $? "bad value ${enableval} for --enable-video" "$LINENO" 5  ;;
+		*) as_fn_error $? "bad value ${enableval} for --enable-video" "$LINENO" 5 ;;
 		esac
 else
   video=true
@@ -16425,7 +16327,7 @@ if test "${enable_x11+set}" = set; then :
   enableval=$enable_x11; case "${enableval}" in
 			  yes) enable_x11=true ;;
 			  no)  enable_x11=false ;;
-			  *) as_fn_error $? "bad value ${enableval} for --disable-x11" "$LINENO" 5  ;;
+			  *) as_fn_error $? "bad value ${enableval} for --disable-x11" "$LINENO" 5 ;;
 		  	  esac
 else
   enable_x11=true
@@ -16499,7 +16401,7 @@ if test "${enable_alsa+set}" = set; then :
   enableval=$enable_alsa; case "${enableval}" in
         yes) alsa=true ;;
         no)  alsa=false ;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-alsa" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-alsa" "$LINENO" 5 ;;
       esac
 else
   alsa=true
@@ -16511,7 +16413,7 @@ if test "${enable_artsc+set}" = set; then :
   enableval=$enable_artsc; case "${enableval}" in
         yes) artsc=true ;;
         no)  artsc=false ;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-artsc" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-artsc" "$LINENO" 5 ;;
       esac
 else
   artsc=false
@@ -16523,7 +16425,7 @@ if test "${enable_portaudio+set}" = set; then :
   enableval=$enable_portaudio; case "${enableval}" in
         yes) portaudio=true ;;
         no)  portaudio=false ;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-portaudio" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-portaudio" "$LINENO" 5 ;;
       esac
 else
   portaudio=false
@@ -16718,7 +16620,7 @@ and MEDIASTREAMER_LIBS to avoid the need to call pkg-config.
 See the pkg-config man page for more details.
 
 To get pkg-config, see <http://pkg-config.freedesktop.org/>.
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 else
 	MEDIASTREAMER_CFLAGS=$pkg_cv_MEDIASTREAMER_CFLAGS
 	MEDIASTREAMER_LIBS=$pkg_cv_MEDIASTREAMER_LIBS
@@ -16754,7 +16656,7 @@ else
 fi
  ;; #(
   *) :
-    as_fn_error $? "bad value '${enable_external_mediastreamer}' for --enable-external-mediastreamer" "$LINENO" 5  ;;
+    as_fn_error $? "bad value '${enable_external_mediastreamer}' for --enable-external-mediastreamer" "$LINENO" 5 ;;
 esac
 
 
@@ -16836,7 +16738,7 @@ if test "${enable_external_ortp+set}" = set; then :
   enableval=$enable_external_ortp; case "${enableval}" in
         yes) external_ortp=true ;;
         no)  external_ortp=false ;;
-        *) as_fn_error $? "bad value ${enableval} for --enable-external-ortp" "$LINENO" 5  ;;
+        *) as_fn_error $? "bad value ${enableval} for --enable-external-ortp" "$LINENO" 5 ;;
       esac
 else
   external_ortp=false
@@ -16925,7 +16827,7 @@ and ORTP_LIBS to avoid the need to call pkg-config.
 See the pkg-config man page for more details.
 
 To get pkg-config, see <http://pkg-config.freedesktop.org/>.
-See \`config.log' for more details" "$LINENO" 5 ; }
+See \`config.log' for more details" "$LINENO" 5; }
 else
 	ORTP_CFLAGS=$pkg_cv_ORTP_CFLAGS
 	ORTP_LIBS=$pkg_cv_ORTP_LIBS
@@ -18077,7 +17979,7 @@ do
     "linphone.iss") CONFIG_FILES="$CONFIG_FILES linphone.iss" ;;
     "po/stamp-it") CONFIG_COMMANDS="$CONFIG_COMMANDS po/stamp-it" ;;
 
-  *) as_fn_error $? "invalid argument: \`$ac_config_target'" "$LINENO" 5 ;;
+  *) as_fn_error $? "invalid argument: \`$ac_config_target'" "$LINENO" 5;;
   esac
 done
 
@@ -18393,7 +18295,7 @@ do
   esac
   case $ac_mode$ac_tag in
   :[FHL]*:*);;
-  :L* | :C*:*) as_fn_error $? "invalid tag \`$ac_tag'" "$LINENO" 5 ;;
+  :L* | :C*:*) as_fn_error $? "invalid tag \`$ac_tag'" "$LINENO" 5;;
   :[FH]-) ac_tag=-:-;;
   :[FH]*) ac_tag=$ac_tag:$ac_tag.in;;
   esac
@@ -18421,7 +18323,7 @@ do
 	   [\\/$]*) false;;
 	   *) test -f "$srcdir/$ac_f" && ac_f="$srcdir/$ac_f";;
 	   esac ||
-	   as_fn_error 1 "cannot find input file: \`$ac_f'" "$LINENO" 5 ;;
+	   as_fn_error 1 "cannot find input file: \`$ac_f'" "$LINENO" 5;;
       esac
       case $ac_f in *\'*) ac_f=`$as_echo "$ac_f" | sed "s/'/'\\\\\\\\''/g"`;; esac
       as_fn_append ac_file_inputs " '$ac_f'"
@@ -18448,7 +18350,7 @@ $as_echo "$as_me: creating $ac_file" >&6;}
 
     case $ac_tag in
     *:-:* | *:-) cat >"$tmp/stdin" \
-      || as_fn_error $? "could not create $ac_file" "$LINENO" 5  ;;
+      || as_fn_error $? "could not create $ac_file" "$LINENO" 5 ;;
     esac
     ;;
   esac
diff --git a/configure.ac b/configure.ac
index fe39916..7e86300 100644
--- a/configure.ac
+++ b/configure.ac
@@ -292,14 +292,6 @@ fi
 
 AC_WORDS_BIGENDIAN
 
-dnl normaly this should only by done by mediastreamer2/configure.ac
-dnl but to workaround bugs when cross-compiling for arm-linux,
-dnl we need to have SPEEX_LIBS defined
-dnl Furthermore it is good to repeat here all mediastreamer2 toggles
-dnl since top-level configure --help will not print them.
-
-PKG_CHECK_MODULES(SPEEX, speex >= 1.1.6, build_speex=yes)
-
 dnl conditionnal build of video support
 AC_ARG_ENABLE(video,
 		[  --enable-video    Turn on video support compiling],

PACKAGE = libmad
VERSION = 0.15.1b

LOCATION = ftp://ftp.mars.org/pub/mpeg
TARBALLS = $(PACKAGE)-$(VERSION).tar.gz

include packages/autotools.mk

prunefiles = \
	$(prefix)/lib/libmad.la

stripfiles = \
	$(prefix)/lib/libmad.so.0.2.1

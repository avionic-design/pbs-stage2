PACKAGE = libid3tag
VERSION = 0.15.1b

LOCATION = ftp://ftp.mars.org/pub/mpeg
TARBALLS = $(PACKAGE)-$(VERSION).tar.gz

include packages/autotools.mk

prunefiles = \
	$(prefix)/lib/libid3tag.la

stripfiles = \
	$(prefix)/lib/libid3tag.so.0.3.0

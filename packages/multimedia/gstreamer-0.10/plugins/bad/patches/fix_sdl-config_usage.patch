From 41d9a3b3aa697989616e240893b0e72df301dd9e Mon Sep 17 00:00:00 2001
From: Soeren Grunewald <soeren.grunewald@avionic-design.de>
Date: Tue, 3 Jul 2012 10:26:45 +0200
Subject: [PATCH] Fix sdl-config usage.

When --with-sdl-prefix is used, we needs to pass the this correctly to
sdl-config as --prefix. Due some changed variable names this was broken.

This patch will correct this and use the right variable names in
m4/gst-sdl.m4 file.
---
 configure     |    6 +++---
 m4/gst-sdl.m4 |    6 +++---
 2 files changed, 6 insertions(+), 6 deletions(-)

diff --git a/configure b/configure
index 35799d4..71a2a0e 100755
--- a/configure
+++ b/configure
@@ -40234,9 +40234,9 @@ $as_echo_n "checking for SDL - version >= $min_sdl_version... " >&6; }
   if test "$SDL_CONFIG" = "no" ; then
     no_sdl=yes
   else
-    SDL_CFLAGS=`$SDL_CONFIG $sdlconf_args --cflags`
-    SDL_LIBS=`$SDL_CONFIG $sdlconf_args --libs`
-    SDL_STATIC_LIBS=`$SDL_CONFIG $sdlconf_args --static-libs`
+    SDL_CFLAGS=`$SDL_CONFIG $sdl_args --cflags`
+    SDL_LIBS=`$SDL_CONFIG $sdl_args --libs`
+    SDL_STATIC_LIBS=`$SDL_CONFIG $sdl_args --static-libs`
 
 
     sdl_major_version=`$SDL_CONFIG $sdl_args --version | \
diff --git a/m4/gst-sdl.m4 b/m4/gst-sdl.m4
index 6073b96..587d268 100644
--- a/m4/gst-sdl.m4
+++ b/m4/gst-sdl.m4
@@ -49,9 +49,9 @@ AC_ARG_ENABLE(sdltest,
   if test "$SDL_CONFIG" = "no" ; then
     no_sdl=yes
   else
-    SDL_CFLAGS=`$SDL_CONFIG $sdlconf_args --cflags`
-    SDL_LIBS=`$SDL_CONFIG $sdlconf_args --libs`
-    SDL_STATIC_LIBS=`$SDL_CONFIG $sdlconf_args --static-libs`
+    SDL_CFLAGS=`$SDL_CONFIG $sdl_args --cflags`
+    SDL_LIBS=`$SDL_CONFIG $sdl_args --libs`
+    SDL_STATIC_LIBS=`$SDL_CONFIG $sdl_args --static-libs`
 
 
     sdl_major_version=`$SDL_CONFIG $sdl_args --version | \
-- 
1.7.10.4


diff --git a/ltmain.sh b/ltmain.sh
index a72f2fd..64bf27a 100755
--- a/ltmain.sh
+++ b/ltmain.sh
@@ -2230,6 +2230,10 @@ func_mode_install ()
 	dir="$dir$objdir"
 
 	if test -n "$relink_command"; then
+	  if test -n "$sysroot"; then
+	    libdir=`$ECHO "$libdir" | $SED -e "s%^$sysroot%%"`
+	    relink_command=`$ECHO "$relink_command" | $SED -e "s%(\(.*\))%(\1 -L$destdir)%"`
+	  fi
 	  # Determine the prefix the user has applied to our future dir.
 	  inst_prefix_dir=`$ECHO "$destdir" | $SED -e "s%$libdir\$%%"`
 
@@ -4687,6 +4691,13 @@ func_mode_link ()
 	  prev=
 	  continue
 	  ;;
+	sysroot)
+	  compiler_flags="$compiler_flags --sysroot $qarg"
+	  sysroot="$qarg"
+	  prev=
+	  func_append compile_command " --sysroot $qarg"
+	  continue
+	  ;;
 	weak)
 	  weak_libs="$weak_libs $arg"
 	  prev=
@@ -5004,6 +5015,11 @@ func_mode_link ()
 	continue
 	;;
 
+      --sysroot)
+	prev=sysroot
+	continue
+	;;
+
       -thread-safe)
 	thread_safe=yes
 	continue
@@ -8505,7 +8521,11 @@ dlopen='$dlfiles'
 dlpreopen='$dlprefiles'
 
 # Directory that this library needs to be installed in:
-libdir='$install_libdir'"
+libdir='$sysroot$install_libdir'"
+	  if test -n "$sysroot"; then
+	    $ECHO >> $output "\
+sysroot=\"$sysroot\""
+	  fi
 	  if test "$installed" = no && test "$need_relink" = yes; then
 	    $ECHO >> $output "\
 relink_command=\"$relink_command\""

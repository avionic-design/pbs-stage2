include packages/libs/libgpg-error/Makefile.location
include packages/autotools-reconf.mk

config-scriptfiles = \
	$(prefix)/bin/gpg-error-config

prunefiles = \
	$(prefix)/lib/*.la

stripfiles = \
	$(prefix)/bin/gpg-error \
	$(prefix)/lib/*.so.*

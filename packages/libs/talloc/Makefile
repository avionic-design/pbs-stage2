PACKAGE = talloc
VERSION = 2.0.8
LICENSE = LGPL

LOCATION = http://samba.org/ftp/$(PACKAGE)
TARBALLS = $(PACKAGE)-$(VERSION).tar.gz

include packages/autotools-reconf.mk

stripfiles = \
	$(prefix)/lib/*.so.*

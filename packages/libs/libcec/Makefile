PACKAGE = libcec
VERSION = git

LOCATION = git://github.com/Pulse-Eight/libcec.git

include packages/autotools.mk

#conf-args 

prunefiles = \
	$(prefix)/lib/*.la

stripfiles = \
	$(prefix)/bin/cec-client \
	$(prefix)/lib/*.so.*

diff --git a/builds/unix/ltmain.sh b/builds/unix/ltmain.sh
index a72f2fd..64bf27a 100755
--- a/builds/unix/ltmain.sh
+++ b/builds/unix/ltmain.sh
@@ -2009,6 +2009,9 @@ func_mode_install ()
 	dir="$dir$objdir"
 
 	if test -n "$relink_command"; then
+	  if test -n "$sysroot"; then
+	    libdir=`$ECHO "X$libdir" | $Xsed -e "s%^$sysroot%%"`
+	  fi
 	  # Determine the prefix the user has applied to our future dir.
 	  inst_prefix_dir=`$ECHO "X$destdir" | $Xsed -e "s%$libdir\$%%"`
 
@@ -4367,6 +4370,13 @@ func_mode_link ()
 	  prev=
 	  continue
 	  ;;
+	sysroot)
+	  compiler_flags="$compiler_flags --sysroot $qarg"
+	  sysroot="$qarg"
+	  prev=
+	  func_append compile_command " --sysroot $qarg"
+	  continue
+	  ;;
 	weak)
 	  weak_libs="$weak_libs $arg"
 	  prev=
@@ -4679,6 +4689,11 @@ func_mode_link ()
 	continue
 	;;
 
+      --sysroot)
+	prev=sysroot
+	continue
+	;;
+
       -thread-safe)
 	thread_safe=yes
 	continue
@@ -8166,7 +8181,11 @@ dlopen='$dlfiles'
 dlpreopen='$dlprefiles'
 
 # Directory that this library needs to be installed in:
-libdir='$install_libdir'"
+libdir='$sysroot$install_libdir'"
+	  if test -n "$sysroot"; then
+	    $ECHO >> $output "\
+sysroot=\"$sysroot\""
+	  fi
 	  if test "$installed" = no && test "$need_relink" = yes; then
 	    $ECHO >> $output "\
 relink_command=\"$relink_command\""

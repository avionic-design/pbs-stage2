config PACKAGE_LIBS_LIBLOGGING
	bool "liblogging"
	select VIRTUAL_LIBC
	select PACKAGE_CORE_SYSTEMD if PACKAGE_LIBS_LIBLOGGING_SYSTEMD

config PACKAGE_LIBS_LIBLOGGING_SYSTEMD
	bool "systemd support"
	depends on PACKAGE_LIBS_LIBLOGGING

PACKAGE = evtest
VERSION = 1.25

LOCATION = http://cgit.freedesktop.org/~whot/$(VERSION)/snapshot/
TARBALLS = $(PACKAGE)-$(VERSION).tar.bz2

include packages/autotools.mk

stripfiles = \
	$(prefix)/bin/evtest \
	$(prefix)/bin/evtest-capture

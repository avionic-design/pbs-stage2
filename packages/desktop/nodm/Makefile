PACKAGE = nodm
VERSION = 0.13

LOCATION = https://github.com/spanezz/$(PACKAGE)/archive
TARBALLS = $(VERSION).tar.gz;filename=$(PACKAGE)-$(VERSION).tar.gz

include packages/autotools.mk

stripfiles = \
	$(prefix)/sbin/nodm

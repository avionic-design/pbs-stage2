PACKAGE = evdev-calibrate
VERSION = 0.1.4

LOCATION = http://ftp.avionic-design.de/pub/$(PACKAGE)
TARBALLS = $(PACKAGE)-$(VERSION).tar.xz

include packages/autotools.mk

stripfiles = \
	$(prefix)/bin/evdev-calibrate

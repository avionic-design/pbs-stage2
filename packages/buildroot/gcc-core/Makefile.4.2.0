# build instructions for bootstrap GCC 4.2.0

conf-args := \
	--prefix=$(ROOTFS)/bootstrap \
	--with-headers=$(ROOTFS)$(prefix)/include \
	--with-build-time-tools=$(ROOTFS)$(prefix)/bin \
	--disable-multilib \
	--disable-threads \
	--disable-nls \
	--enable-symvers=gnu \
	--disable-shared \
	--enable-languages=c

conf-vars :=

package-configure:
	mkdir -p $(pkgtree)/obj-$(HOST) && cd $(pkgtree)/obj-$(HOST) && \
		$(env) ../configure $(conf-args) $(conf-vars)

build-args :=
build-vars :=

package-build:
	cd $(pkgtree)/obj-$(HOST) && \
		$(env) $(MAKE) $(build-args) $(build-vars) all-gcc

install-args :=
install-vars :=

package-install:
	cd $(pkgtree)/obj-$(HOST) && \
		$(env) $(MAKE) $(install-args) $(install-vars) install-gcc


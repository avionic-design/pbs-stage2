PACKAGE = gcc
VERSION = $(GCC_VERSION)

LOCATION = ftp://ftp.gnu.org/gnu/$(PACKAGE)/$(PACKAGE_FULLNAME)
TARBALLS = $(PACKAGE)-core-$(VERSION).tar.bz2

include packages/buildroot/common.mk
include packages/buildroot/gcc/Makefile.$(VERSION)


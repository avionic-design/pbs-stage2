PACKAGE = gdb
VERSION = 6.8

LOCATION = http://ftp.gnu.org/gnu/$(PACKAGE)
TARBALLS = $(PACKAGE_FULLNAME).tar.bz2

##############################################################################

include packages/common.mk

package-configure:

package-build:

package-install:

package-clean:
	@:


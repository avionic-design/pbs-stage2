PACKAGE = tmux
VERSION = 1.7
LICENSE = BSD

LOCATION = http://downloads.sourceforge.net/$(PACKAGE)
TARBALLS = $(PACKAGE)-$(VERSION).tar.gz

include packages/autotools-reconf.mk

stripfiles = \
	$(prefix)/bin/tmux

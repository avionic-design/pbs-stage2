PACKAGE = tmux
VERSION = 2.4
LICENSE = BSD

LOCATION = https://github.com/$(PACKAGE)/$(PACKAGE)/releases/download/$(VERSION)
TARBALLS = $(PACKAGE)-$(VERSION).tar.gz

include packages/autotools-reconf.mk

stripfiles = \
	$(prefix)/bin/tmux

PACKAGE = bridge-utils
VERSION = 1.5
LICENSE = GPL

LOCATION = http://prdownloads.sourceforge.net/bridge
TARBALLS = $(PACKAGE)-$(VERSION).tar.gz

include packages/autotools-reconf.mk

stripfiles = \
	$(prefix)/sbin/brctl

PACKAGE = sshfs-fuse
VERSION = 2.4

LOCATION = http://sourceforge.net/projects/fuse/files/$(PACKAGE)/$(VERSION)
TARBALLS = $(PACKAGE)-$(VERSION).tar.gz

include packages/autotools-reconf.mk

stripfiles = \
	$(prefix)/bin/sshfs

PACKAGE = sshfs-fuse
VERSION = 2.5

LOCATION = https://github.com/libfuse/sshfs/releases/download/sshfs_2_5/
TARBALLS = $(PACKAGE)-$(VERSION).tar.gz

include packages/autotools-reconf.mk

stripfiles = \
	$(prefix)/bin/sshfs

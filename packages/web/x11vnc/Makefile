PACKAGE = x11vnc
VERSION = 0.9.14

LOCATION = https://github.com/LibVNC/$(PACKAGE)/archive
TARBALLS = $(VERSION).tar.gz;filename=$(PACKAGE)-$(VERSION).tar.gz

include packages/autotools-reconf.mk

stripfiles = \
	$(prefix)/bin/x11vnc

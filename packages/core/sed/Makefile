PACKAGE = sed
VERSION = 4.2.1

LOCATION = http://ftp.gnu.org/gnu/$(PACKAGE)
TARBALLS = $(PACKAGE)-$(VERSION).tar.bz2

include packages/autotools.mk

conf-args += \
	--prefix=/

stripfiles = \
	/bin/sed

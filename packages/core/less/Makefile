PACKAGE = less
VERSION = 451
LICENSE = GPL

LOCATION = http://ftp.gnu.org/gnu/$(PACKAGE)
TARBALLS = $(PACKAGE)-$(VERSION).tar.gz

include packages/autotools.mk

stripfiles = \
	$(prefix)/bin/less \
	$(prefix)/bin/lessecho \
	$(prefix)/bin/lesskey

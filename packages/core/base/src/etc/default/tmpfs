# size and mode of the /tmp filesystem
TMPFS_SIZE=2M
TMPFS_MODE=0755

# size and mode of the /var/lock filesystem
LOCK_SIZE=2M
LOCK_MODE=1777

# size and mode of the /var/run filesystem
RUN_SIZE=2M
RUN_MODE=0755


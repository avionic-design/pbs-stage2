TARGET := $(CONFIG_ARCH)-

ifneq ($(CONFIG_CPU),)
  TARGET := $(TARGET)$(CONFIG_CPU)-
endif

TARGET := $(TARGET)$(CONFIG_OS)-
TARGET := $(TARGET)$(CONFIG_LIBC)-

ifneq ($(CONFIG_ABI),)
  TARGET := $(TARGET)$(CONFIG_ABI)-
endif

export TARGET

